{# {% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>User</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ task.id }}</td>
            </tr>
            <tr>
                <th>Task</th>
                <td>{{task.name}}</td>
            </tr>
             <tr>
                <th>Author</th>
                <td>{{user.email}}</td>
            </tr> 
        </tbody>
    </table>

    <a href="{{ path('app_index') }}">back to list</a>

    <a href="{{ path('app_user_edit', {'id': task.id}) }}">edit</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %} #}
<h1>{{task.getAuthor().getEmail()}}</h1>
<h2>{{task.name}}</h2>
{% for comment in comments %}

<p>{{ comment.text }}</p>
    
{% endfor %}