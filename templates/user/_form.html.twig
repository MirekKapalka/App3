{{ form_start(form,{'attr': {'class':'w-2/3 grid grid-cols-1 align-items-center'}}) }}
    {{form_widget(form, {'attr': {'class': "col-span-3 h-7 w-full float-right bg-zinc-100 border-2 border-black rounded text-center mb-2", 'placeholder':'Nazwa'}} ) }}
   
    <button class="btn bg-zinc-300 hover:bg-zinc-400 w-1/2 place-self-center border-2 border-black rounded">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
